# visualizador-perfil-github

Endpoints que iremos usar

Perfil: GET /users/{username}

Repositórios: GET /users/{username}/repos (usar query per_page-10 e sort-updated / sort-pushed conforme objetivo )